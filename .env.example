SECRET_KEY=change-me-generate-a-new-random-value
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Set to True only once the site is served over HTTPS (e.g. behind nginx with a cert)
SECURE_SSL_REDIRECT=False
